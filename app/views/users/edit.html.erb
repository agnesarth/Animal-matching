<div class="container">
  <div class="mt-5 col-md-6 offset-md-3 shadow p-3 mb-5 rounded bg-devise container-pet shadow text-center">
    <i class="fa fa-paw mt-2 fa-5x"></i>
    <h3 class="mt-3 mb-3">Editer mon profil</h3>
    <a tabindex="0" class="btn btn btn-outline-primary btn-info-match" role="button" data-html="true" data-toggle="popover" data-trigger="focus" title="À quoi servent vos informations personnelles ?" data-content="Vos noms et prénoms seront donnés uniquement aux profils avec lesquels vous avez matchés, ce qui est plus personnel que de transmettre uniquement votre adresse email. <br>Quant à vos données géographiques, elles vous permettent de trouver des <i>amimaux</i> près de chez vous."> Pourquoi donner mes infos ? </a>
    <div class="text-center my-2">
      <%= form_for(current_user, url: users_path(current_user), method: :put) do |f| %>
        <div class="form-inline">
          <div class="col-6 col-first">
            <h6 class="card-text mt-2">Prénom :</h6>
            <div class="input-group mb-4">
              <%= f.text_field :first_name, placeholder: @user.first_name, class:"form-control" %>
            </div>
          </div>
          <div class="col-6 col-second">
            <h6 class="card-text mt-2">Nom :</h6>
            <div class="input-group mb-4">
              <%= f.text_field :last_name, placeholder: @user.last_name, class:"form-control" %>
            </div>
          </div>
        </div>
        <hr>
        <h6 class="card-text mt-4">Adresse :</h6>
        <div class="input-group mb-4">
          <%= f.text_field :address, placeholder: @user.address, class:"form-control" %>
        </div>
        <div class="form-inline">
          <div class="col-6 col-first">
            <h6 class="card-text mt-2">Ville :</h6>
            <div class="input-group mb-4">
              <%= f.text_field :city, placeholder: @user.city, class:"form-control" %>
            </div>
          </div>
          <div class="col-6 col-second">
            <h6 class="card-text mt-2">Pays :</h6>
            <div class="input-group mb-4">
              <%= f.text_field :country, placeholder: @user.country, class:"form-control" %>
            </div>
          </div>
        </div>
        <hr>
        <%if @user.default_pet_id != nil %> 
          <h6 class="card-text mt-4">Animal en vedette :</h6>
          <div class="input-group mb-5">
            <%= f.select :default_pet_id, current_user.pets.collect { |p| [ p.name, p.id ] }, include_blank: true, class:"form-control" %>
          </div>
        <% end %>
        <div class="actions">
          <%= f.submit "Mettre à jour", class:"btn btn-primary" %>
        </div>
      <% end %>
    </div>
    <div class="mt-5">
      <%= link_to 'Ajouter un animal', new_pet_path %><br>
    </div>
  </div>
  <div class="container font-weight-bold mb-5">
    <%= link_to "Retour", :back %>
  </div>
</div>
