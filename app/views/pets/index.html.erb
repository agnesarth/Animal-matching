<div class="small-jumbotron text-center ">

</div>


<div class="container">
  <!-- Search bar disabled for now, need to implement post-MVP -->
  <input class="form-control" id="anythingSearch" type="text" placeholder="Type something to search list items" disabled>
  <div id="myDIV" class="my-3">
    <button class="btn btn-outline-primary btn-main-page" disabled>Rechercher</button>
    <button class="btn btn-info" disabled>Another button</button>
    <hr />
    <p></p>
  </div>
</div>

 <!-- Search bar in progress -->
  <h4>Search</h4>
  <%= form_tag(pets_path, method: :get) do%>
    <%= text_field_tag(:search, params[:search])%>
    <%= submit_tag ("Search") %>
  <% end %>

  <%=@pets.each do | pet | %>
  <ul>
    <li><%= link_to pet.name, pet_path(@pet)%></li><br>
  </ul>
  <% end %>
   <!-- Search bar in progress -->

<div class="container justify-content-around">
  <div class="row justify-content-around ml-3 mr-3">
    <% @pets_list.each do |pet| %>
      <%unless Like.where(liker_id: @current_pet.id, liked_id: pet.id).exists?%>
        <div class="col-sm-4">
          <div id="likeCard<%= pet.id %>" class="card bg-light mb-3">
            <div id="likeHeader<%= pet.id %>" class="card-header">
              <h5 class="card-title float-left"><%= pet.name%></h5>
            </div>
            <div id="likeBody<%= pet.id %>" class="card-body">
              <div id="demo" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner" >
                  <% unless pet.photos.empty? %>
                    <div class="carousel-item active">
                      <%= image_tag pet.photos.first, class: "card-img-top", style: "height: 300px" %>
                    </div>
                    <% pet.photos[1..-1].each do |p| %>
                      <div class="carousel-item">
                        <%= image_tag p, class: "card-img-top", style: "height: 300px" %>
                      </div>
                    <% end %>
                  <% else %>
                    <div class="carousel-item active">
                      <%= image_tag("paw.svg", {class: "card-img-top", style: "height: 300px"} ) %>
                    </div>
                    <div class="carousel-item">
                      <%= image_tag("paw.svg", {class: "card-img-top", style: "height: 300px"} ) %>
                    </div>
                  <% end %>
                </div>
              </div>
          <!---------------------CARD BODY END--------------------->
              <div class="mt-3">
                <h6 class="card-subtitle my-3 text-primary"><em>"<%= pet.short_description%>"</em></h6>
                <h6 class="card-subtitle mt-2 float-left"><%= pet.age.to_i %>, <%= pet.sex %></h6><span>
                <%= link_to pet_likes_path(pet.id), method: :post, remote: true do %>
                  <i id="likeButton<%= pet.id %>" class="btn btn-outline-primary btn-sm fa fa-heart float-right"></i>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <%end%>
    <% end %>
  </div>
</div>
