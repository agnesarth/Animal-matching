<div class="small-jumbotron text-center ">
    <div class="container">
      <!--<input class="form-control" id="anythingSearch" type="text" placeholder="Type something to search list items">
      <div id="myDIV">
        <button class="btn btn-outline-primary btn-main-page">Rechercher</button>
        <button class="btn btn-info">Another button</button>
        <hr />
        <p></p>
      </div>-->
    </div>
</div>

<% unless current_user.pets.present? %>
<div class="container text-center">
<h1>C'est parti!</h1>

<h3>La recherche de ton amour en fourrure commence ici</h3>
<%= link_to 'Ajoute ton animal single', new_pet_path, class: "btn btn-lg btn-outline-warning" %>
</div>

<div class="bubbling-heart">
  <div><i class="fa fa-heart fa-5x"></i></div>
  <div><i class="fa fa-heart fa-5x"></i></div>
  <div><i class="fa fa-heart fa-5x"></i></div>
  <div><i class="fa fa-heart fa-5x"></i></div>
  <div><i class="fa fa-heart fa-5x"></i></div>
  <div><i class="fa fa-heart fa-5x"></i></div>
</div>
<% else %>


<div class="container text-center">
<h1>Pets</h1>
</div>

    <% Pet.all.where.not(user_id: current_user.id).each do |pet| %>
      <tr>
        <td><%= pet.name %></td>
        <td><%= pet.animal %></td>
        <td><%= pet.chip_number %></td>
        <td><%= pet.breed %></td>
        <td><%= pet.sex %></td>
        <td><%= pet.age %></td>
        <td><%= pet.photos.count %></td>
        <td><%= link_to 'Show', pet %></td>
        <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
        <td><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>



<% end %>
